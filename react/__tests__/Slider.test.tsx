describe('Basic rendering', () => {
  it.todo(
    'should render complete slider, with arrows and pagination dots on default settings'
  )
  it.todo(
    'should render without pagination features is usePagination is set to false'
  )
  it.todo(
    'should render as static list if there are not enough slides to fill a page'
  )
  it.todo('should add padding in the x-axis if slider is not a full width one')
})

describe('Accessibility', () => {
  it.todo('should have correct aria attributes')
})

describe('Behavior upon interaction', () => {
  it.todo(
    'should correctly toggle arrows and pagination dots visibility according to device'
  )
})
